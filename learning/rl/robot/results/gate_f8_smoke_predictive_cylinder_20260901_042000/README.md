# Superseded F8 smoke invocation

This first 16-step invocation initialized and stepped PhysX successfully but
used the completion-only evaluator contract, so it intentionally produced no
evaluation CSV.  It performed no training and is excluded from F8 evidence.
The subsequent timestamped smoke uses the explicit `--smoke-only` contract.

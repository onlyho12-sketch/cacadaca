# Superseded diagnostic interpretation

The PhysX run completed and exactly matched the vertical flat baseline, but the
reported alignment error used link6 local `+Z`.  The collected tool's outward
pad approach axis is link6 local `-Z`, as demonstrated by the flat identity
pose.  This directory is preserved but excluded from F5 aggregation; a new run
uses the corrected Gate-F-only axis diagnostic.

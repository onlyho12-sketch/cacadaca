# Gate F5 bounded freeform trace probe

This intentionally bounded 500-step invocation verified that corrected `xyzw`
normal alignment runs normally on the freeform mesh.  It stopped only at the
configured step limit and is excluded from final aggregation.

# Superseded alignment-axis diagnostic

The physical run completed safely, but its reported alignment error used a
link6 `-Z` proxy that does not represent both tilt axes of the actual contact
disk.  The corrected diagnostic measures pad-body local `+Y` directly.  This
run is preserved and excluded from final F5 aggregation.

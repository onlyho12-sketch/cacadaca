# 새채팅 인수인계서 — 평면 관측 확장·경로/공정 최적화·곡면 선택 이식

작성일: 2026-08-30  
작업 브랜치: `lr_yj`  
저장소: `/home/rokey/cacadaca`  
상태: **계획 확정, 구현·재학습 시작 전**

이 문서는 다음 작업자가 현재 평면 PhysX 결과를 훼손하지 않고, Ra 품질회귀의 원인을
검증한 뒤 관측 확장·상위 공정 최적화·곡면 기능 이식을 순서대로 수행하기 위한 실행용
인수인계서다.

반드시 함께 읽을 선행 문서:

1. `gloss_test/docs/새채팅_전체작업_인수인계서.md`
2. `gloss_test/docs/새채팅_PhysX접촉력_D단계_인수인계서.md`
3. 이 문서 전체

---

## 1. 최종 목표

최종 목표는 단순히 모든 합성 표면을 억지로 통과시키는 것이 아니다.

1. 3D 스캔과 결함 지도로 안전한 기본 폴리싱 경로를 만든다.
2. IK·충돌·도달 가능성 검사를 통과한 경로에서 로봇을 움직인다.
3. 하위 PPO가 순간 접촉력과 이송속도를 숙련공처럼 조절한다.
4. pass 종료 검사 결과를 보고 상위 공정 제어기가 다음 pass의 RPM, step-over, 경로 방향,
   가장자리 보정, 기본 힘·속도 및 중단 여부를 정한다.
5. GU, Ra, Rz, scratch, clearcoat, 힘, 온도를 동시에 만족하면 성공으로 판정한다.
6. 안전하게 더 개선할 수 없는 구역은 무한 반복하지 않고 현재 공정의 한계와 다음 조치
   (다른 pad/compound, 국부 샌딩, clearcoat 보수·재도장 등)를 사용자에게 알린다.
7. 평면이 안정된 뒤 같은 구조를 보디 라인·곡면으로 확장한다.

즉 숙련공 기술 저장의 대상은 `성공 동작`뿐 아니라 `마무리 전환`, `안전 중단`,
`공정 변경 판단`까지다.

---

## 2. 절대 보호 대상과 작업 규칙

### 2.1 수정 금지

다음은 읽기 전용 참고만 한다.

- `scripts/polishing_v5.py`
- `scripts/polishing_v5_modules/`
- 공용 `learning/rl/env/polish_env.py`
- 공용 `learning/rl/env/polish_env_cfg.py`

필요 기능은 Robot 전용 subclass, 새 helper module, 새 평가/학습 script에 구현한다.

### 2.2 결과 보존

- 기존 checkpoint, CSV, 로그를 삭제·이름변경·덮어쓰기하지 않는다.
- 새 실험은 새 출력 prefix와 새 디렉터리를 사용한다.
- 기존 11/100, 18/100, finish-grid 결과는 PPT용 발전 과정 데이터다.
- 코드를 바꾸기 전과 각 실험 후 `git status --short`를 기록한다.

### 2.3 사용자 협업 규칙

- 작업을 시작하기 전에 할 일, 수정 대상, 예상시간을 사용자에게 설명한다.
- 사용자가 실행을 승인한 뒤에만 코드 수정·장시간 실험을 시작한다.
- 장시간 작업은 단계별 중간 결과와 남은 시간을 보고한다.
- 실험 시작 명령, PID/로그/출력 경로와 재개 방법을 남긴다.

---

## 3. 현재 확정 상태

### 3.1 평면 기준과 챔피언

- PhysX ContactSensor 사용
- 기준 이송속도: `12.7 mm/s`
- PPO feed residual 범위: `6.35~19.05 mm/s`
- clearcoat 잔량 안전선: `30 μm`
- `q_clearcoat`는 GU 감점에서 분리하고 안전 진단에만 사용
- 힘 hard limit: `14 N`
- 현재 평면 챔피언:
  `learning/rl/robot/champion/model_bc_robot_tesla12p7.pt`
- BC 데이터: 200환경 × 5,500 step = 1,100,000 sample
- PPO: 200환경 × 400 iter × 48 step = 3.84M sample
- PPO 후보는 같은 100표면에서 현재 BC를 넘지 못했다.

### 3.2 100표면 결과

`100표면`은 서로 다른 차량 부위 100개가 아니다. 동일한 `12×12 cm` 수평 평면 patch를
100환경에 만들고 scratch, 초기 미세거칠기, clearcoat 상관장 seed를 다르게 한 것이다.

| 항목 | 결과 |
|---|---:|
| 1 pass 성공 | 11/100 |
| 2 pass 추가 성공 | 7/89 |
| 최종 성공 | **18/100** |
| `fail_quality_regression` | **82/100** |
| 성공 평균 pass | 1.39 |
| 최종/최선보존 전체 GU 평균 | 70.23 |
| clearcoat 안전 | 100/100, 최소 36.12 μm |
| 최대 힘 / 14 N 초과 | 13.20 N / 0회 |
| sensor fallback | 0회 |
| 최고온도 | 48.01 °C |

상태기계는 최대 6 pass를 허용했지만 실제 흐름은 다음과 같다.

- 100개 모두 1차 실행
- 1차 미통과 89개만 2차 실행
- 2차에서 7개 추가 성공
- 나머지 82개는 품질회귀 guard로 중단
- **3차 pass는 한 표면도 실행하지 않았다.**

### 3.3 Ra 회귀

| 단계 | GU 평균 | Ra 평균 | Scratch 평균 |
|---|---:|---:|---:|
| pass 1 | 66.91 | 0.199 μm | 1.019 μm |
| 계속 진행한 89개의 pass 2 | 70.13 | **0.271 μm** | 0.545 μm |

GU와 scratch는 좋아졌지만 Ra가 악화됐다. 힘 초과, clearcoat 부족, sensor fallback이 직접
원인은 아니다.

현재 확인된 코드 수준 직접 원인은 다음과 같다.

- 최종 높이맵은 초기 미세요철·scratch와 비균일 누적 제거량을 함께 포함한다.
- 레스터 footprint 중첩이 만든 큰 높이 편차가 Ra 계산에 들어갈 수 있다.
- 같은 BC는 1차와 2차를 구분할 관측을 받지 못한다.
- 따라서 scratch를 더 지우면서 경로 띠·중앙/가장자리 제거량 편차를 키울 수 있다.

아직 확정하지 않은 것은 `중앙 과제거가 Ra 회귀의 전부`라는 주장이다. 다음 단계에서
미세 Ra, 경로 waviness, 누적 제거량 지도를 분리해 검증해야 한다.

### 3.4 중단된 finish-grid 실험

현재 `learning/rl/repolish_eval.py`에는 승인된 미완료 실험 변경이 남아 있다.

- 새 `checkpoint_finish` 모드
- pass 1은 챔피언 BC 유지
- pass 2부터 controlled finish force/feed로 교체

완료된 16표면 조건:

| pass 2+ 조건 | 최종 통과 | 안전 위반 |
|---|---:|---:|
| force action `-1.0`, feed `12.7 mm/s` | 3/16 | 0 |
| force action `-1.0`, feed `8.0 mm/s` | **5/16** | 0 |

현재 실행 중인 학습·평가 프로세스는 없다. 이 결과는 원인 진단 전의 중간 screen이므로
삭제하지 않되, 바로 100표면으로 확대하지 않는다.

### 3.5 현재 작업트리에서 반드시 보존할 변경

```text
 M learning/rl/repolish_eval.py
?? learning/rl/robot/results/repolish_tesla12p7_bc_finish_hybrid_smoke_1env.csv
?? learning/rl/robot/results/repolish_tesla12p7_bc_finish_hybrid_smoke_1env_passes.csv
?? learning/rl/robot/results/repolish_tesla12p7_bc_finish_hybrid_smoke_1env_tiles.csv
?? learning/rl/robot/results/repolish_tesla12p7_bc_finishgrid_fm1p0_v12p7_16env.csv
?? learning/rl/robot/results/repolish_tesla12p7_bc_finishgrid_fm1p0_v12p7_16env_passes.csv
?? learning/rl/robot/results/repolish_tesla12p7_bc_finishgrid_fm1p0_v12p7_16env_tiles.csv
?? learning/rl/robot/results/repolish_tesla12p7_bc_finishgrid_fm1p0_v8p0_16env.csv
?? learning/rl/robot/results/repolish_tesla12p7_bc_finishgrid_fm1p0_v8p0_16env_passes.csv
?? learning/rl/robot/results/repolish_tesla12p7_bc_finishgrid_fm1p0_v8p0_16env_tiles.csv
```

이 변경을 임의로 되돌리거나 덮어쓰지 않는다.

---

## 4. 현재 평면과 경로의 기하학적 문제

### 4.1 현재 수치

- 품질 patch: `120×120 mm`
- 품질 격자: `2 mm`, 즉 60×60 = 3,600점
- 패드 직경/반경: `110/55 mm`
- step-over ratio: `0.184`
- 레스터 간격: `20.24 mm`
- 레스터 line: 6개
- line y: 약 10.12, 30.36, 50.60, 70.84, 91.08, 111.32 mm
- x 중심 경로: 0→120 mm 또는 120→0 mm
- 첫 점: 왼쪽 아래 경계 부근 `(0, 10.12 mm)`

현재도 패드 전체가 품질 patch 안에 있지 않다. x 끝점에서는 패드 절반이 품질영역 밖으로
나간다. 이것 자체는 평가 셀이 실제 패널 끝이 아니라면 자연스럽다.

문제는 현재 PhysX Workpiece가 `160×160 mm`, 즉 품질 patch보다 사방 20 mm만 크다는 점이다.
패드는 품질 경계에서 반경 55 mm까지 뻗으므로 일부가 실제 Workpiece 밖에도 걸친다.
품질 모델은 patch 밖 footprint를 잘라 계산하므로 연속 차체의 접촉과 완전히 일치하지 않는다.

### 4.2 사용자와 최종 합의한 경계 원칙

`패드 전체를 품질 셀 내부에 제한`하지 않는다. 원형 패드를 사각 셀 내부에만 두면 꼭짓점이
가공되지 않으며 지금까지의 경로와도 달라진다.

대신 다음 두 경계를 분리한다.

1. **평가 셀 경계**: 가상의 검사 경계. 패드가 넘어가도 된다.
2. **실제 패널 끝·틈·단차**: 물리적 경계. 별도 edge 안전규칙과 필요 시 작은 패드가 필요하다.

평면 내부 셀 실험에서는 더 큰 연속 물리 평판과 전체 품질 상태맵을 만들고, 가운데 ROI의
품질을 판정한다. 패드가 ROI 밖으로 나가도 전체 패드는 연속된 물리 차체 위에 있어야 한다.

초기 검토 후보:

- 전체 물리/품질 상태맵: `300×300 mm`
- 중앙 판정 ROI: `200×200 mm`
- 상태맵 전체의 scratch/removal/clearcoat를 계산
- ROI만 최종 품질 집계

이 수치는 확정 상수가 아니라 첫 geometry/coverage test 후보이며, 구현 전 메모리·시간과
패드 coverage를 계산해 확정한다.

### 4.3 현재 경로의 예비 coverage 계산

정책 action과 scratch 선택 제거를 제외하고 현 레스터와 제거 footprint만 계산한 예비 결과:

| 위치 | 평균 대비 상대 제거 노출량(현재 제거식 압력 보정 포함) |
|---|---:|
| 중앙 40×40 mm | 약 1.15 |
| 좌우 가장자리 | 약 0.87 |
| 아래/위 가장자리 | 약 0.85/0.89 |
| 네 모서리 | 약 0.73~0.77 |

중앙이 모서리보다 약 50~58% 큰 노출량을 받을 수 있다. 이는 원인 후보를 지지하지만 정식
PhysX/quality paired 결과는 아니다. 다음 단계에서 실제 `cumulative_removal_um`과 타일별
미세 Ra/waviness로 재검증한다.

단순히 시작점을 모서리로 바꾸는 것은 해결책이 아니다. 현재 경로도 왼쪽 아래 경계
부근에서 시작하며, 같은 line 집합을 순서만 바꾸면 최종 누적 coverage는 거의 같다.

---

## 5. Scratch 범위에 관한 확정 캐비어트

현재 우리와 비교 Git 모두 다음 생성기를 쓴다.

- scratch depth: `0.05~2.0 μm` 균일분포
- 개수: 4~12
- 폭: 2 mm (`PT-DESIGN`)
- 길이: 30~120 mm (`PT-DESIGN`)

문헌의 `0.05~2.0 μm`는 세차 브러시로 손상된 clearcoat 표본에서 관찰한 마·미세 scratch의
포괄 범위이며, 신차 실제 발생 분포가 아니다. 개별 미세 scratch가 밀집하면 swirl 외관으로
보일 수 있지만, 범위 전체를 `신차 swirl 분포`라고 부르면 안 된다.

`0.05~0.50 μm`는 경미한 swirl/mar 실험군 후보로 합리적이지만, 기존 범위를 단순 교체하면
균일분포 평균 깊이가 1.025→0.275 μm로 약 73% 감소해 시험 자체가 크게 쉬워진다.

따라서 후속 원칙:

- 경미군 `0.05~0.50 μm`
- 중간/깊은 교정군 `0.50~2.0 μm`
- 혼합비는 실측이 없으므로 `PT-DESIGN`
- 난이도군별 통과율을 별도 보고
- 기존 결과와 직접 비교할 때 분포 변경 caveat 명시

Scratch 범위 변경만으로 Ra 모델의 미세거칠기/경로 waviness 혼합 문제는 해결되지 않는다.

---

## 6. 다음 PPO에 필요한 정보와 표현 방식

사용자가 요구한 정보:

1. 현재 pass 번호
2. 현재 전체 GU
3. 현재 Ra·Rz
4. clearcoat 남은 예산
5. 2차원 scratch 지도
6. 2차원 누적 제거량 지도
7. 중앙·가장자리 제거량 차이
8. 아직 덜 닦인 타일 위치
9. 이미 과도하게 닦인 타일 위치

### 6.1 전역 scalar 관측 후보

- normalized `pass_idx`
- 직전 검사 `GU_target - GU`
- 직전 검사 `Ra - Ra_target`
- 직전 검사 `Rz - Rz_target`
- 전역 최소 clearcoat 안전여유
- 전역 누적 제거량 평균
- 제거량 균일도(CV 또는 max-min)
- 중앙 평균 제거량 − 가장자리 평균 제거량
- 공정 stage: correction/finishing

`현재 pass 번호`와 `재폴리싱 횟수`는 거의 중복이므로 normalized pass 하나부터 쓴다.
공정 stage는 상태기계가 정의한 값일 때만 명시적으로 넣는다.

### 6.2 공간 관측

전체 고해상도 지도는 환경 상태와 CSV에 보존한다. PPO MLP에는 우선 기존 타일 체계와 맞춘
5×5 축약 지도를 넣는다.

- scratch 5×5
- cumulative removal 5×5
- under-polished mask 5×5
- over-polished mask 5×5

8·9번은 별도 물리장이 아니라 scratch/removal/target/clearcoat에서 파생한 mask다.
중앙/edge 이진 플래그보다 경계까지 정규화 거리, planned coverage, 실제 dwell/removal을
사용하는 편이 낫다.

### 6.3 실기 적용 가능성

시뮬레이션에서 매 0.05초 전체 GU/Ra/Rz를 다시 계산해 PPO에 주면 실제 로봇에 없는 oracle이
될 수 있다. 전역 GU/Ra/Rz는 pass 종료 검사에서 갱신하고 다음 pass 동안 고정한다. 작업 중에는
접촉력, 국소 scratch/removal, clearcoat model estimate, 온도 등을 갱신한다.

### 6.4 먼저 할 ablation

관측을 한꺼번에 확장한 모델 하나만 학습하지 않는다.

1. 기존 14ch baseline
2. 기존 14ch + pass/전역 품질 scalar
3. 위 scalar + 5×5 공간지도

같은 짧은 학습·같은 seed로 비교해 가장 나은 구성을 본 학습한다. 비교 Git의 단순 4채널
lookahead 실험은 99→95/150로 악화됐으므로 `공간정보를 넣으면 무조건 좋아진다`고 가정하지 않는다.

---

## 7. 행동과 상·하위 제어 구조

### 7.1 첫 재학습 행동

첫 재학습에서는 행동을 유지한다.

```text
Action
├─ ΔForce
└─ ΔFeed
```

- ΔForce: base force의 ±30%
- ΔFeed: base feed의 ±50%
- 현재 12.7 mm/s 기준 실제 feed 6.35~19.05 mm/s

관측 확장만으로 pass-aware finishing이 가능한지 먼저 검증한다.

### 7.2 바로 PPO 행동으로 넣지 않을 항목

- RPM: 모터 관성·열 때문에 pass/line 단위 상위 변수로 시작
- step-over: 다음 line 위치를 정하므로 line/pass 단위 상위 변수
- 자유 `Δx, Δy, Δz`: 행동공간·안전 위험이 크므로 보류
- 법선 위치와 force를 동시에 자유 행동으로 주면 두 제어가 충돌할 수 있음

### 7.3 최종 계층 구조

```text
3D Scan / 결함 지도
        ↓
안전한 기본 경로 생성
        ↓
IK / Collision / Reachability
        ↓
상위 공정 제어기 (pass 또는 line 단위)
 ├─ RPM
 ├─ Step-over
 ├─ 경로 방향
 ├─ Edge 보정
 ├─ Base force/feed
 └─ Stop/Continue
        ↓
하위 PPO (20 Hz)
 ├─ ΔForce
 └─ ΔFeed
        ↓
PhysX 폴리싱 → 검사 → 다음 pass
```

기존 BO는 고정 recipe 탐색기다. 바로 온라인 상위 제어기가 되는 것은 아니다. 상태별 offline
BO/contextual BO/대리모델로 pass recipe library를 만들고, 처음에는 명시적 상태기계가 그중
하나를 고르는 방법이 시간과 안전 면에서 유리하다.

경로 residual RL은 마지막 단계에서 작은 접선 방향 오프셋, line 종료 시 다음 step-over 선택
정도로 제한해 도입한다. 기준 경로는 항상 유지하고 안전 projection을 통과시킨다.

---

## 8. Step-over와 경로 실험 계획

현재 step-over `0.184`는 과거 약 5.95 mm/s 해석식 BO 조건에서 나온 값이다. 현재의
12.7 mm/s·PhysX·반복 폴리싱 조건에서 재최적화되지 않았다.

첫 screen 후보:

| ratio | line 간격 | 기하학적 겹침률 |
|---:|---:|---:|
| 0.18 | 19.8 mm | 82% |
| 0.25 | 27.5 mm | 75% |
| 0.32 | 35.2 mm | 68% |
| 0.40 | 44.0 mm | 60% |

경로 후보:

- 1차 가로 raster → 2차 같은 방향
- 1차 가로 → 2차 세로 cross-raster
- edge correction line 포함/미포함
- 필요 시 pass별 line 순서 변경(열 순서 효과 확인용)

모든 후보는 같은 seed로 먼저 비학습/고정정책 16표면 paired screen을 한다. 평가항목:

- GU, 미세 Ra/Rz, scratch
- 누적 제거량 평균·CV·max-min
- 중앙/edge 제거량 차이
- 타일 under/over count
- clearcoat 최소와 소모량
- raw/filtered/used force, 14 N 초과
- 최고온도·열손상
- 공정시간

상위 1~2개만 100표면 평가 또는 새 BC/PPO에 사용한다.

---

## 9. 새 BC/PPO 계획

관측 차원이 바뀌면 기존 checkpoint를 그대로 새 네트워크에 load할 수 없다.

1. 기존 챔피언은 보존하고 teacher action 생성에 사용한다.
2. 새 관측을 기록하는 BC dataset을 별도 경로로 수집한다.
3. pass 1 teacher는 현 챔피언 행동을 이용할 수 있다.
4. pass 2+ teacher에는 선정된 finishing recipe/상태기계 행동을 포함한다.
5. 모방 MSE, 센서 fault, 접촉률, pass/stage 분포를 보고한다.
6. 짧은 observation ablation 뒤 최종 구성만 100~200환경 PPO로 본 학습한다.
7. checkpoint는 iteration별 보존하고 8→16→100 같은 seed로 선택한다.

본 평가 기준은 평균 reward가 아니라 다음 우선순위다.

1. 전체 동시통과 수
2. 힘·clearcoat·온도 안전
3. Ra 회귀 감소
4. 제거량 균일도
5. GU 및 scratch 개선
6. 공정시간

---

## 10. 비교 Git에서 선택 이식할 기능

비교 저장소:
`https://github.com/seb000423/cacadaca.git`

확인 branch/commit:

- branch: `learning-bc-v2`
- commit: `ea9d030ea2c89e85b6678d90fc94f8b65066f430`
- commit message: `Repolish x curved x physics first judgment: lever works (+14 GU best), 0/8 pass`

### 10.1 가져올 가치가 있는 것

- `make_curved_patch`: cylinder/sphere/freeform 형상과 법선 생성
- `curve_height_normal`: 경로 높이와 국소 법선 계산
- 41×41 trimesh PhysX 곡면 Workpiece 생성 구조
- 로봇 IK 목표 Z를 곡면 높이에 연결하는 구조
- 접촉력을 국소 표면 법선에 투영하는 구조
- 평면정책×평면/곡면, 곡면정책×평면/곡면의 2×2 paired 평가 방식
- observation profile/ablation harness의 실험 구조

공용 base 파일 전체를 복사하지 않는다. 새 Robot 전용 geometry/helper module과 adapter로
필요 코드만 이식하고, 단위시험을 함께 가져오거나 새 기준에 맞게 작성한다.

### 10.2 가져오지 않을 것

- 과거 약 5.95 mm/s 곡면 checkpoint
- `model_ppo_curved.pt`를 우리 챔피언으로 사용
- 저쪽 clearcoat 35 μm + GU 결합 설정
- 힘 초과가 발생한 저쪽 재폴리싱 로직
- 공용 `polish_env.py`/cfg 전체 덮어쓰기
- 저쪽 raster path(우리와 같은 edge/coverage 문제를 해결하지 않음)
- 99/150 해석식 결과를 우리 PhysX 100표면과 직접 비교

### 10.3 저쪽 결과의 정확한 해석

- 차량 해석식 export: 99/150 = 66% 동시통과
- 이는 PhysX 접촉 100표면 결과가 아니다.
- 저쪽 평면/곡면 PhysX 2×2는 n=4 소표본이고 전체통과 0/4
- 곡면 재폴리싱 8회는 전체통과 0/8
- 최고 곡면 재폴리싱은 GU 53.57→67.60, Ra 0.1484, Rz 1.372,
  scratch 1.814→0.078, clearcoat 43.21이지만 GU<70 및 force overload로 실패
- 최신 `ea9d030`은 코드 변경이 아니라 문서와 결과 CSV 추가

따라서 저쪽은 곡면 인프라가 앞섰지만 최종 PhysX 품질·재폴리싱 안전이 우리보다 완성된
것은 아니다.

---

## 11. 실행 순서와 게이트

### Gate A — 동결과 진단

1. 기존 결과 manifest와 checksum/경로 기록
2. 현재 dirty worktree 보존 확인
3. GPU/Isaac Sim 단일환경 최소 스모크
4. 기존 챔피언 1환경 회귀 확인

완료조건: 기존 파일 무손실, 스모크 안전, 새 출력 prefix 확정.

### Gate B — 표면/측정/coverage

1. 큰 연속 상태맵 + 중앙 ROI 구조 설계
2. PhysX Workpiece와 quality 상태맵 좌표 일치 검증
3. 패드 footprint가 Workpiece 밖으로 나가지 않는지 검증
4. 미세 Ra/Rz와 제거 waviness/균일도 분리
5. 타일별 scratch/removal/under/over 기록

완료조건: 중앙/edge 제거량 차이와 pass 2 Ra 회귀 원인을 같은 seed에서 설명 가능.

### Gate C — step-over/path screen

1. 4개 step-over
2. same-direction/cross-direction
3. edge correction 후보
4. 16표면 paired screen

완료조건: 안전을 지키면서 제거 균일도와 Ra가 좋아지는 상위 1~2개 조건 선정.

### Gate D — 관측 ablation과 새 BC/PPO

1. scalar-only와 spatial 관측 비교
2. 새 pass-aware BC
3. 짧은 PPO ablation
4. 최종 100~200환경 학습

완료조건: 동일 100표면에서 기존 18/100과 비교하고 Ra 회귀·안전·시간을 함께 보고.

### Gate E — 상위 공정 제어

1. pass recipe library 생성
2. 상태기계가 correction/finishing/stop 선택
3. 필요 시 contextual BO/대리모델로 확장

완료조건: 1차·2차·3차가 서로 다른 목적과 recipe를 사용하고 품질회귀 시 안전 중단.

### Gate F — 곡면 선택 이식

1. geometry/normal/mesh helper 이식
2. unit test와 PhysX cylinder smoke
3. 12.7 mm/s·30 μm·우리 힘 제한으로 새 BC/PPO
4. flat/cylinder/sphere 2×2 이상 paired 평가

완료조건: 저쪽 checkpoint 결과가 아니라 우리 기준의 새 곡면 결과 생성.

---

## 12. 예상시간

GPU와 Isaac Sim이 안정적이라는 조건의 범위다.

| 작업 | 예상 |
|---|---:|
| 결과 동결·기초 회귀 | 0.5~1시간 |
| 큰 평판/ROI·Ra/coverage 진단 | 2~4시간 |
| step-over/path 16표면 screen | 3~5시간 |
| 관측 확장·시험 | 3~6시간 |
| BC/PPO·100표면 재평가 | 6~12시간 |
| 평면 단계 합계 | 약 12~24시간 |
| 곡면 helper 이식·첫 재학습 포함 | 약 24~40시간 |

GPU Xid 또는 Isaac Sim 중단이 재현되면 시간을 숨기지 말고 즉시 현재 checkpoint·CSV·로그와
재시작 조건을 보고한다. `nvidia-smi`가 응답한다는 것만으로 장시간 학습 안정성을 확정하지 않는다.

---

## 13. 새 채팅에서 그대로 붙여넣을 첫 주문문

```text
/home/rokey/cacadaca/gloss_test/docs/새채팅_전체작업_인수인계서.md와
/home/rokey/cacadaca/gloss_test/docs/새채팅_PhysX접촉력_D단계_인수인계서.md,
그리고
/home/rokey/cacadaca/gloss_test/docs/새채팅_평면관측확장_경로최적화_인수인계서.md를
처음부터 끝까지 읽고 현재 상태를 이어받아줘.

기존 scripts/polishing_v5.py, scripts/polishing_v5_modules/,
공용 learning/rl/env/polish_env.py와 polish_env_cfg.py는 수정하지 말고 참고만 해.
기존 체크포인트·CSV·PPT용 실험 결과와 현재 작업트리 변경사항도 삭제하거나 덮어쓰지 마.

먼저 문서와 실제 코드·결과 파일이 일치하는지 읽기 전용으로 확인하고,
네가 이해한 현재 상태, 보호 대상, 남은 작업, 예상시간을 설명해.
바로 코드 수정이나 학습을 시작하지 말고 내 승인을 기다려.
항상 작업을 시작하기 전에 무엇을 할지와 예상시간부터 알려줘.
```

새 작업자가 이해 내용을 정확히 보고한 뒤 두 번째로 다음을 보낸다.

```text
인수인계 내용대로 Gate A부터 시작해.
기존 결과와 현재 dirty worktree를 먼저 동결·보존하고,
큰 연속 물리 평판과 중앙 평가 ROI, 미세 Ra/Rz와 제거량 불균일도 분리,
step-over·교차경로 진단 순서로 진행해.

공용 polish_env.py/cfg와 polishing_v5는 수정하지 말고 Robot 전용 새 모듈을 사용해.
각 gate 시작 전에 수정 파일, 실행 명령, 예상시간을 설명하고,
각 gate 결과는 새 CSV·문서에 남긴 뒤 다음 gate로 넘어가기 전에 보고해.
```

---

## 14. 현재 한 줄 상태

> 12.7 mm/s 평면 PhysX 챔피언은 BC이며 1-pass 11/100, 반복 후 18/100이다. 미통과 82개는
> pass 2에서 GU·scratch는 개선되지만 Ra가 0.199→0.271 μm로 회귀해 안전 중단됐고,
> 3차는 실행하지 않았다. 현재는 finish-grid 2조건까지만 보존된 채 실행이 멈춰 있다.
> 다음은 무작정 재학습이 아니라 큰 연속 평판/중앙 ROI와 Ra-waviness 분리, step-over·교차경로
> screen, pass/global/map 관측 확장, 새 BC→PPO, 상위 pass optimizer, 곡면 선택 이식 순서다.

# Incomplete invocation

The process exited immediately after environment initialization and before the
first control-step progress marker.  It produced no result artifacts.  This
empty attempt is preserved and excluded from Gate F5 aggregation; a new output
directory is used for the retry.

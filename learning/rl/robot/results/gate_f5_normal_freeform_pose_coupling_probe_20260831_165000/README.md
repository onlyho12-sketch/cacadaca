# Gate F5 bounded pose-coupling probe

This intentionally bounded 1000-step run verified that target-orientation
translation coupling passes the former freeform overload point at step 702.
It stopped only at the configured limit and is excluded from final aggregation.
